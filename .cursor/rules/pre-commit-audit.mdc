---
description: Git pre-commit 全库工作区深度审计
alwaysApply: true
---

# 提交门禁（全库 · 工作区）

```
Cursor Commit / git commit
  → scripts/audit/cursor-git   # 丢掉静默 --no-verify
  → .githooks/pre-commit
  → scripts/audit/run.sh --commit
       → scan.sh → build.sh → report → confirm.sh
```

## 布局

```
scripts/audit/
  run.sh       # 编排：默认只检查；--commit 含二次确认
  lib.sh       # 日志 / PATH / hook 调用记录
  scan.sh      # 暂存一致性、密钥、冲突、JS、shellcheck
  build.sh     # deep.py + 临时 hugo/glossary + html.mjs
  confirm.sh   # macOS 弹窗
  install.sh   # hooksPath + git.path
  cursor-git   # Cursor Source Control 包装器
  deep.py / html.mjs
.githooks/pre-commit
```

## 启用

```bash
npm run hooks:install
# Command Palette → Developer: Reload Window
pip3 install pyyaml   # 若尚未安装
npm run audit         # 试跑（不弹窗）
```

## 行为

- 审计对象是**磁盘完整工作区**（含未暂存）；已暂存又有未暂存改动 → 失败
- 生产构建写到**临时目录**，不碰本地 `public/`
- 跳过：`SKIP_COMMIT_AUDIT=1`
- 真要 `--no-verify`：`ALLOW_GIT_NO_VERIFY=1`

## 诊断

```bash
tail .git/hook-invocations.log    # hook/审计是否进过
tail .git/cursor-git-args.log     # 若有 DROPPED_NO_VERIFY = 面板曾静默跳过
```

## 本地预览

词条高亮是构建期 `glossary-inject` 写入的。用 `npm run dev`（不要裸 `hugo server`）。
