---
description: Git pre-commit 全库工作区深度审计
alwaysApply: true
---

# 提交门禁（全库 · 工作区）

Cursor **Commit** → `.githooks/pre-commit` → `scripts/audit/run.sh`。

审计对象是**磁盘上的完整工作区**（含未暂存）。目的：保证全库随时可构建、可发布。

额外约束：若某文件**已暂存但仍有未暂存修改**，直接失败——防止「工作区审计通过、实际提交内容却不同」。

生产构建审计写到 **临时目录**（`$TMP/public-audit`），**不碰、不删本地 `public/`**，可与 `npm run dev` 并行。

## 布局

```
scripts/audit/
  run.sh     # 编排入口
  deep.py    # 结构/内容一致性
  html.mjs   # 构建后 HTML 断链等
scripts/git-pre-commit-audit.sh  # 兼容旧路径 → run.sh
.githooks/pre-commit
```

## 启用

```bash
npm run hooks:install
pip3 install pyyaml
# 可选：brew install shellcheck
npm run audit          # 本地试跑（跳过 macOS 对话框需非 Darwin 或 CONFIRM_COMMIT）
```

## 检查项

1. 禁止跟踪 `public/`、`node_modules/`
2. 可疑密钥文件名（跟踪 + 源码树未跟踪）
3. 冲突标记；`git diff --check`（工作区 + 暂存）
4. 密钥/私钥内容形态
5. 全库 JS/MJS `node --check`
6. shellcheck（已安装时）
7. Python：YAML/JSON、nav、glossary、mermaid、MD 站内链、大文件、front matter
8. **临时目录** hugo（Pages `HUGO_BASEURL`）+ glossary-inject（跳过 pagefind）
9. 对该临时产物做 HTML 审计
10. Hugo `--printPathWarnings`（并入步骤 8）
11. macOS 二次确认

报告：`.git/last-pre-commit-audit.txt`、`.git/last-pre-commit-build.log`

跳过：`SKIP_COMMIT_AUDIT=1`

## 本地预览（构建期注入）

词条高亮是 **构建期** `glossary-inject` 写入 HTML 的。

- 推荐：`npm run dev`（固定 **1313**；启动前会清掉旧 dev/占用，避免 Hugo 自动换端口）
- 换端口：`PORT=1314 npm run dev`
- 不要裸跑 `hugo server`：重建会覆盖 `public/`，泡泡会全部消失
- 生产：`npm run build`（含 inject + pagefind）
- 提交审计与本地预览互不抢 `public/`
