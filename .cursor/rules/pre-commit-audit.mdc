---
description: Git pre-commit 提交审计（术语泡泡 + 空路径 + 二次确认）
alwaysApply: true
---

# 提交门禁

```
git commit
  → .githooks/pre-commit
  → node scripts/audit/run.mjs --commit
       → glossary-check → links-check → build-temp → bubbles-check → confirm
```

## 布局

```
scripts/audit/
  run.mjs              # 入口；默认只检查；--commit 含 macOS 确认
  glossary-check.mjs   # 词库 summary / source / link / aliases
  links-check.mjs      # nav book + content 站内链接 / ref
  build-temp.mjs       # 临时目录 hugo + glossary-inject
  bubbles-check.mjs    # HTML .glossary-term / .glossary-tip
  confirm.mjs          # macOS 弹窗
  lib.mjs
  install.sh
.githooks/pre-commit
```

## 启用

```bash
npm run hooks:install
npm run audit              # 试跑（不弹窗）
npm run audit:precommit    # 含确认弹窗
```

## 行为

- 纯代码，不走 AI
- 全站临时构建，不碰本地 `public/`
- 跳过：`SKIP_COMMIT_AUDIT=1`
- 无 `git.path` / cursor-git 包装器（Cursor 若带 `--no-verify` 会绕过）

## 诊断

```bash
cat .git/last-pre-commit-audit.txt
tail .git/last-pre-commit-build.log
```

## 本地预览

词条高亮是构建期 `glossary-inject` 写入的。用 `npm run dev`（不要裸 `hugo server`）。
