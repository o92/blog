---
description: 站点信息架构、三栏布局与明暗主题约定
alwaysApply: true
---

# Site layout & IA

完整设计见：`docs/superpowers/specs/2026-08-06-site-ia-layout-design.md`

## 结构（勿改成「分类进 content 文件夹」）

- 顶栏 Logo 文案为「首页」，链接用 `site.Home.RelPermalink`
- 本地 `baseURL` 为 `http://localhost:1313/`；GitHub Pages 由 CI `HUGO_BASEURL`（或 `npm run build:pages`）设为 `https://o92.github.io/blog/`，避免本地资源跑到 `/blog/` 前缀下打不开
- **分类按 BoK**：一级=专业 BoK，**二级起=KA/主题**；细则与新增确认流程见 `bok-taxonomy.mdc`
- **一级分类**是目录；**非一级**无子分类且只有一本书时，节点本身就是书（`book: <content目录名>` 或 `books: […]`，一点进入）；多本或有 `children` 才展开；导航节点不需要 `id`
- 书与章节：`content/<book-id>/`（无 `content/books/` 中间层）
- 书阅读壳：`layouts/partials/book-shell.html`（`single` / `list` 共用）
- 书根 `_index.md`：`book_title` = 书名（面板标题、面包屑书根等）；`title` = 本页标题（正文 h1、**左栏目录顶项**）；缺省 `book_title` 时回退 `title`
- **最终页**：`final = true` 即可；子文合并进本页，直链子页会跳到 `#part-…`（无需手写 cascade）
- 自建 `layouts/`，不引入 Docsy 等主体主题

## 版式

书内：顶栏（宽度 / 搜索 / 主题）+ 左书目 + 中 `.post-content` + 右本章 TOC。  
栏宽常量：`static/js/layout-widths.js`（首屏与 `width.js` 共用）。  
搜索：Pagefind；见 `docs/superpowers/specs/2026-08-06-search-pagefind-design.md`。

## 明暗主题

- 默认跟 `prefers-color-scheme`；右上角仅浅/深
- 偏好 `sessionStorage`；关浏览器后清空
- 颜色走 CSS 变量；`:root` 浅色 + dark media/`data-theme=dark`（无需再抄一份 light）

## 与 glossary / 构建

- `npm run build`：hugo → inject → pagefind
- 完整预览：`npm run preview`
