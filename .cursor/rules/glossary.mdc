---
description: Hugo 术语词库、构建后处理与悬停气泡约定
alwaysApply: true
---

# Glossary（术语自动强调）

完整设计见：`docs/superpowers/specs/2026-08-06-glossary-tooltip-design.md`

## 构建

`npm run build` = `hugo` → `glossary-inject` → `pagefind`。  
`hugo server` **不会**注入术语或建搜索索引；完整预览用 `npm run preview`。

## 词库与声明

- `data/glossary/global.yaml` + `data/glossary/<domain>.yaml`
- 词条：`summary` 必填；`source`（站内摘录 +「显示更多」）；`link`（仅外链）；`aliases`（同义中文/别名，共用同一气泡）
- 文章 `glossary: ["domain"]`；未声明时仅 `global`
- **继承**：子页 / 最终页会合并祖先 section `_index` 上的 `glossary`（在书根声明即可覆盖全书）
- 正文选择器：默认仅 `.post-content`（`scripts/glossary.config.json`）

## 匹配语义（勿擅自改；要改先更新本规则 + design spec）

- 跳过 `pre` / `code` / `kbd` / `samp` / `script` / `style` 及已处理术语
- 长短优先；英文大小写不敏感；每次出现都高亮
- 全局与领域重名时气泡并显

## 运行时气泡

- `static/js/glossary-tip.js`：fixed 浮层、避顶栏、贴边自适应、桥接热区
- 样式：`static/css/glossary.css`（跟站点 CSS 变量）
- `baseof` 已引入 glossary CSS；inject **不再**二次插入 stylesheet

## 改动边界

- 增删术语：`data/glossary/*.yaml`
- 注入：`scripts/glossary-inject.mjs` / `glossary.config.json`
- CI：`.github/workflows/hugo.yaml` 使用 `npm run build`
