---
description: 笔记章节结构：每章第一小节必须是名词定义
alwaysApply: true
---

# 章节首节 = 名词定义

新增或拆分**笔记章节**（`content/<笔记>/…` 下一章/一部分）时：

1. **第一小节永远是名词介绍**（文件名常用 `01-terms.md`，`title = "名词介绍"`，`weight = 1`）。
2. 专名以 **英文** 为标题（`### Term`），下列中文、简称、释义；并写入对应 `data/glossary/<域>.yaml`，`source` 指向该节标题。
3. 同章后续正文再展开；勿把首节做成导读长文而把词条挤到后面。
4. 已有章节若缺名词节，补篇时优先补上，而不是只改后文。

示例顺序：`01-terms` → `02-…` → `03-…`。
