+++
title = "AIP-147: 敏感字段"
weight = 19
+++

**英文标题**：Sensitive fields  
**原文**：[AIP-147](https://google.aip.dev/147) · approved · 创建 2020-07-24  
**许可**：原文 © Google，[CC BY 4.0](https://creativecommons.org/licenses/by/4.0/)

API 有时要收集敏感信息：底层服务需要**存**，但因为数据敏感，写进去之后不打算再**读**出来。例如私有加密密钥。这类数据在请求和响应里怎么露面，要额外想清楚。

## 能写不能读

敏感信息对资源整体存在是**必需的**时，数据应当作为 input-only 字段接受（见 [AIP-203]({{< ref "aip-0203" >}})），不要再配一个对应的输出字段。资源要存在就必须有这份敏感数据，所以用户可以假定：资源在，敏感数据就已经存上了。例如：

```proto
message SelfManagedKeypair {
  string name = 1 [(google.api.field_behavior) = IDENTIFIER];

  // The public key data in PEM-encoded form.
  bytes public_key = 2;

  // The private key data in PEM-encoded form.
  bytes private_key = 3 [
    (google.api.field_behavior) = INPUT_ONLY];
}
```

敏感信息在包含它的资源里是**可选的**时，应当再用一个带 `_set` 后缀的 output-only 布尔（见 [AIP-203]({{< ref "aip-0203" >}})），表明敏感信息在不在。例如：

```proto
message Integration {
  string name = 1 [(google.api.field_behavior) = IDENTIFIER];
  string uri = 2;

  // A secret to be passed in the `Authorization` header of the webhook.
  string shared_secret = 3 [
    (google.api.field_behavior) = INPUT_ONLY];

  // True if a `shared_secret` has been set for this Integration.
  bool shared_secret_set = 4 [
    (google.api.field_behavior) = OUTPUT_ONLY];
}
```

若重要的是能认出这份敏感信息，但不允许完整读回，可以用一个带 `obfuscated_` 前缀、类型相同的字段，而不是布尔 `_set`，用来提供一点上下文。具体怎么混淆，超出本文范围。例如：

```proto
message AccountRecoverySettings {
  // An email to use for account recovery.
  string email = 1 [
    (google.api.field_behavior) = INPUT_ONLY];

  // An obfuscated representation of the recovery email. For example,
  // `ada@example.com` might be represented as `a**@e*****e.com`.
  string obfuscated_email = 2 [
    (google.api.field_behavior) = OUTPUT_ONLY];
}
```
