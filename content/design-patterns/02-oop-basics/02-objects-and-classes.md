+++
title = "对象与类"
weight = 2
+++

OOP 把**相关数据与行为**捆成 **Object**，由程序员定义的 **Class** 作为蓝图批量构造。

- **字段**承载状态；**方法**定义行为；合称 members。
- 同类实例结构相同、字段值可不同。
- 多类可组成 **Class hierarchy**：共同属性抽到 **Superclass**，差异留在 **Subclass**；子类可覆盖继承来的方法（替换或增强）。

模式目录里的 UML 大量表达的就是：谁继承谁、谁持有谁、谁只依赖接口。
